export const MAX_EMERGENCY_CONTACTS = 5;

export const VOICE_KEYWORD_CONFIDENCE_THRESHOLD = 0.75;
export const SCREAM_PROBABILITY_THRESHOLD = 0.7;
export const SENSOR_EVENT_CONFIDENCE_THRESHOLD = 0.8;

export const COMMUNITY_ALERT_RADIUS_METERS = 800;

export const PAGINATION_DEFAULTS = {
  page: 1,
  limit: 20,
  maxLimit: 100,
} as const;
